[package]
name = "brz-symmetry-web"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
brdb = { version = "0.5", default-features = false, features = ["brz"] }
wasm-bindgen = "0.2"
